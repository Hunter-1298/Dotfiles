# PR B Summary — Remove `/dashboards` plural tree

**PR:** https://github.com/c3-e/c3cdao-cra/pull/178
**Branch:** `agent/hunter-poc-dashboards-sweep-20260507` → `hunter/poc`
**Base commit:** `7092d9347` (PR #177 — agent ported to `/dashboard`)
**Sweep commit:** `dd31d8517`

## Files deleted: 84

| Category                                                                                                        | Count                        |
| --------------------------------------------------------------------------------------------------------------- | ---------------------------- |
| `routes/dashboards/*` + `dashboards.tsx` + `dashboards.$slug.tsx`                                               | 32                           |
| `components/dashboards/*` (orphans) + `__tests__/`                                                              | 22 (16 components + 6 tests) |
| `components/powerbi/admin/*` + `__tests__/`                                                                     | 11 (8 components + 3 tests)  |
| `components/powerbi/{PowerBIEditor,DashboardToolbar,SaveAsModal}` + `wizard/` + tests                           | 8                            |
| `components/home/PinnedDashboards` + test                                                                       | 2                            |
| `components/powerbi/ShareModal` + test                                                                          | 2                            |
| Orphan hooks (`useDashboardInstances`, `useDashboardLayout`, `useDashboardShare`, `usePinnedInstances`) + tests | 6                            |
| `lib/dashboards-search-context.tsx` + test                                                                      | 2                            |
| `routes/__tests__/AppNavRail.test.tsx` (pinned NavRail purpose-built)                                           | 1                            |

## Files modified: 3

| File                | Change                                                                                  |
| ------------------- | --------------------------------------------------------------------------------------- |
| `routes/__root.tsx` | Dropped pinned-dashboards NavRail group, `usePinnedInstances` import, unused `Pin` icon |
| `routes/index.tsx`  | Dropped `<PinnedDashboards />` render block + import                                    |
| `routeTree.gen.ts`  | Auto-regenerated by `@tanstack/router-plugin`                                           |

## Audit table — components kept (not deleted)

| Path                                               | Importer outside deleted tree                                                                            |
| -------------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| `components/dashboards/MockFinancialDashboard.tsx` | `components/powerbi/PowerBIEmbed.tsx` (mock-mode)                                                        |
| `components/dashboards/LivePowerBIEmbed.tsx`       | `components/powerbi/PowerBIEmbed.tsx` (lazy live-mode) — **caught on 2nd pass** via dynamic import audit |
| `components/dashboards/agent/*`                    | `routes/dashboard.tsx` (workspace canvas dock)                                                           |
| `components/powerbi/PowerBIEmbed.tsx`              | `routes/admin/power-bi.tsx`                                                                              |
| `hooks/useDashboardAgentPlan.ts`                   | composer (alive)                                                                                         |
| `hooks/useDashboardThumbnail.ts`                   | `components/pbi-canvas/ThumbnailCapture.tsx`                                                             |
| `lib/dashboard-agent-api.ts`                       | composer (alive)                                                                                         |
| `routes/dashboard.$workspaceId.*.tsx`              | Singular `/dashboard/...` workspace report viewer/editor (different namespace)                           |

## TS error counts

| Stage                        | Count |
| ---------------------------- | ----- |
| Baseline (origin/hunter/poc) | 15    |
| Final                        | 15    |
| Delta                        | 0     |

All 15 errors are pre-existing (unrelated to this PR): `@cdao/ui` module resolution + a `report_status` mismatch in `MissionRead`.

## Test counts

| Suite                                                                    | Result                 |
| ------------------------------------------------------------------------ | ---------------------- |
| `pnpm vitest run src/routes/__tests__/ src/components/dashboards/agent/` | 5 files, **40 passed** |
| `uv run ruff check app/` (backend)                                       | clean                  |
| `uv run pytest tests/api/test_dashboard_agent_routes.py -q -m no_db`     | **11 passed**          |

## Net

- **84 files deleted, 3 modified, −13,730 LOC.**
- The dashboard agent dock remains on `/dashboard` (per PR #177).
- Zero backend changes — `cdao_powerbi.routes.instances/templates/permissions` preserved as shared infrastructure.

## Notes for the parent agent

- PR opened, **not admin-merged**. User reviews and merges.
- One miss caught on 2nd pass: `LivePowerBIEmbed` is dynamically imported by `PowerBIEmbed`. Initial static-import grep missed it; restored before commit.
- If user later confirms no other client (CLI, future surfaces) consumes the backend dashboard-instance APIs, a follow-up PR can delete the backend half (`cdao_powerbi.routes.instances/templates/permissions`).
